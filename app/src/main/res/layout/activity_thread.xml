<?xml version="1.0" encoding="utf-8"?>
<!--
  TxxT conversation thread (T2).

  Text-first message lines on AMOLED black (docs/DESIGN.md §"Conversation
  thread"). No chat bubbles, no rounded cards — the no-bubble constraint is
  structural (docs/PRIVACY.md §2). The message list is a RecyclerView filled by
  ThreadAdapter (T3); the compose bar sits at the bottom with a minimal
  attachment affordance and NO voice-message affordance (docs/PRIVACY.md §5 —
  voice messages are never sent or received).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/thread_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000">

    <!-- Message list. Flat text-first rows, never bubbles. -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/message_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="12dp"
        android:clipToPadding="false" />

    <!-- Compose bar: a dictation (mic) affordance, the text field, and minimal
         send/settings affordances. The mic is dictation only — it runs
         SpeechRecognizer and inserts recognized text into the compose field via
         DictationInsert; no audio is ever sent or received (docs/PRIVACY.md §5 —
         voice messages are never sent or received, and dictation does not send
         anything). -->
    <LinearLayout
        android:id="@+id/compose_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#09090B"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <Button
            android:id="@+id/dictation_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:text="Mic"
            android:textColor="#000000"
            android:backgroundTint="#FFFFFF" />

        <EditText
            android:id="@+id/compose_input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Message"
            android:textColor="#E6FFFFFF"
            android:textColorHint="#80FFFFFF"
            android:fontFamily="@font/font_body"
            android:background="#18181B"
            android:padding="10dp"
            android:inputType="textShortMessage"
            android:maxLines="4" />

        <Button
            android:id="@+id/send_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="Send"
            android:textColor="#000000"
            android:backgroundTint="#FFFFFF" />

        <!-- Settings affordance: opens the settings screen (WS12 T5). -->
        <Button
            android:id="@+id/settings_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="⚙"
            android:textColor="#000000"
            android:backgroundTint="#FFFFFF" />
    </LinearLayout>

</LinearLayout>