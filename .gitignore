# Skippy workspace state — never commit
.skippy/
.ws13-measure/

# Build artifacts
build/
.gradle/
local.properties
*.iml
.idea/

# Estate mill/audit scratch — never tracked
.nagatha/
.bilby/

# Mill-seeded venv is a house symlink or share dir — never tracked
.venv
